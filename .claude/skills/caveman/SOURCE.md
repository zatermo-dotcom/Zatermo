# Provenance

| | |
|---|---|
| Upstream | https://github.com/juliusbrussee/caveman |
| Path | `skills/caveman/` |
| Plugin version | 2.7.0 |
| Commit | eae856e |
| License | MIT (see `LICENSE`) |

`SKILL.md` and `README.md` are copied verbatim from upstream. To update, re-copy
those two files from `skills/caveman/` in the upstream repo.

Upstream also ships a `cavecrew` skill, three `cavecrew-*` subagents, and a
Claude Code plugin with `SessionStart`/`UserPromptSubmit` hooks. None of those
are installed here — this is the skill only.
