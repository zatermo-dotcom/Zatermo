# Studio feedback

`/as:studio-feedback` prepares an Architecture Studio bug report or feature request for GitHub. It collects minimal local diagnostics, removes or flags sensitive project information, and shows every proposed field before one confirmation gate.

Opening a prefilled issue transmits the displayed URL fields to GitHub immediately, before the user submits the issue. Architecture Studio never submits an issue automatically. Canceling before the browser opens sends nothing.
